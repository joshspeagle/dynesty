language: python

# Setting sudo to false opts in to Travis-CI container-based builds.
sudo: false

python:
    - 2.7
    - 3.4

env:
    global:
        - SCIPY_VERSION=0.16
        - CMD='./runtests.py'
    matrix:
        - NUMPY_VERSION=1.5  # oldest numpy on conda
        - NUMPY_VERSION=1.9  # latest numpy

matrix:
    include:
        - python: 2.7
          env: NUMPY_VERSION=1.5 SCIPY_VERSION=0.11  # oldest scipy on conda
        - python: 3.4
          env: NUMPY_VERSION=1.9 SCIPY_VERSION=0  # no scipy

before_install:
    - wget http://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh
    - chmod +x miniconda.sh
    - ./miniconda.sh -b
    - export PATH=/home/travis/miniconda/bin:$PATH
    - conda update --yes conda

install:
    - conda create --yes -n test python=$TRAVIS_PYTHON_VERSION pytest
    - source activate test
    - if [[ $SCIPY_VERSION > 0 ]]; then conda install numpy=$NUMPY_VERSION scipy=$SCIPY_VERSION; fi
    - source activate test
    - python setup.py install

script: $CMD
